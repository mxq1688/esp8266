# ESP8266 PlatformIO 项目烧录方式

## 方法一：使用 PlatformIO IDE (推荐)

### 1. 安装 PlatformIO IDE
- 下载并安装 [Visual Studio Code](https://code.visualstudio.com/)
- 在 VS Code 中安装 PlatformIO IDE 扩展

### 2. 打开项目
```bash
# 在 VS Code 中打开 platformio 文件夹
code platformio/
```

### 3. 配置 WiFi
编辑 `src/main.cpp`，修改这两行：
```cpp
const char* ssid = "YOUR_WIFI_SSID";        // 改为你的WiFi名称
const char* password = "YOUR_WIFI_PASSWORD"; // 改为你的WiFi密码
```

### 4. 连接硬件
- 用 USB 线连接 ESP8266 到电脑
- 确保驱动已安装（CH340/CP2102）

### 5. 烧录步骤
1. 点击底部状态栏的 "✓" 图标编译
2. 点击 "→" 图标上传到设备
3. 点击 "🔌" 图标打开串口监视器

## 方法二：使用命令行

### 1. 安装 PlatformIO Core
```bash
# 使用 pip 安装
pip install platformio

# 或使用包管理器
brew install platformio  # macOS
```

### 2. 进入项目目录
```bash
cd platformio/
```

### 3. 编译项目
```bash
pio run
```

### 4. 查看可用端口
```bash
pio device list
```

### 5. 上传到设备
```bash
# 自动检测端口
pio run --target upload

# 或指定端口
pio run --target upload --upload-port /dev/ttyUSB0  # Linux
pio run --target upload --upload-port COM3         # Windows
```

### 6. 监视串口输出
```bash
pio device monitor --port /dev/ttyUSB0 --baud 115200
```
